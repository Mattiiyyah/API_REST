# API REST - Gerenciamento de Alunos

Este projeto é uma **API REST** desenvolvida em Node.js para o gerenciamento de alunos e usuários, incluindo funcionalidades de upload de fotos e autenticação via Token JWT.

O projeto foi desenvolvido como parte do curso de JavaScript e TypeScript do professor **Luiz Otávio Miranda** na Udemy, com o objetivo de solidificar conhecimentos sobre backend, bancos de dados relacionais e padrões de projeto.

## 📋 Funcionalidades

* **Autenticação de Usuários:** Login via JSON Web Token (JWT).
* **CRUD de Usuários:** Criação, leitura, atualização e exclusão de usuários.
* **CRUD de Alunos:** Gerenciamento completo de dados de alunos (Nome, Sobrenome, E-mail, Idade, Peso, Altura).
* **Upload de Arquivos:** Upload de fotos de perfil para os alunos utilizando `Multer`.
* **Segurança:** Hash de senhas com `bcryptjs` e proteção de rotas sensíveis com middleware de login.
* **Banco de Dados:** Integração com MariaDB/MySQL utilizando ORM Sequelize.

## 🚀 Tecnologias Utilizadas

* **Node.js**
* **Express**
* **Sequelize** (ORM)
* **MariaDB / MySQL**
* **JWT** (JSON Web Token)
* **Bcryptjs**
* **Multer**
* **Sucrase**

## 📦 Pré-requisitos

Para rodar o projeto, você precisará ter instalado em sua máquina:
* [Node.js](https://nodejs.org/) (versão LTS recomendada)
* [MariaDB](https://mariadb.org/) ou [MySQL](https://www.mysql.com/)

## 🔧 Instalação e Configuração

1.  **Clone o repositório:**
    ```bash
    git clone https://github.com/Mattiiyyah/API_REST.git
    cd API_REST
    ```

2.  **Instale as dependências:**
    ```bash
    npm install
    ```

3.  **Configure as Variáveis de Ambiente:**
    Crie um arquivo `.env` na raiz do projeto baseado no exemplo abaixo:

    ```env
    DATABASE=nome_do_seu_banco
    DATABASE_HOST=127.0.0.1
    DATABASE_PORT=3306
    DATABASE_USERNAME=seu_usuario
    DATABASE_PASSWORD=sua_senha

    TOKEN_SECRET=sua_chave_secreta_aqui
    TOKEN_EXPIRATION=7d

    APP_URL=http://localhost:3001
    ```

4.  **Execute as Migrations:**
    Para criar as tabelas no banco de dados, execute:
    ```bash
    npx sequelize db:migrate
    ```

5.  **(Opcional) Popule o banco:**
    Se houver seeds configurados para criar usuários iniciais:
    ```bash
    npx sequelize db:seed:all
    ```

## ⚡ Rodando a Aplicação

Para iniciar o servidor em ambiente de desenvolvimento:

```bash
npm run dev
